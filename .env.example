# =============================================================================
# VAULTWARDEN - CONFIGURACIÓN DE VARIABLES
# =============================================================================
# Copia este archivo como .env y rellena los valores.
# Luego cifra con: ./manage_secrets.sh encrypt
# =============================================================================

# --- VAULTWARDEN API ---
# Obtener en: Tu Vaultwarden -> Ajustes -> Seguridad -> Keys
BW_HOST=https://tu-dominio.ejemplo.com
BW_CLIENTID=user.xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
BW_CLIENTSECRET=tu_client_secret_aqui
BW_PASSWORD=tu_contraseña_maestra

# --- CLOUDFLARE TUNNEL ---
# Token del tunnel (Cloudflare Zero Trust -> Tunnels -> Tu Tunnel -> Install)
TUNNEL_TOKEN=tu_token_de_cloudflare_tunnel

# --- TELEGRAM NOTIFICACIONES ---
# Crear bot con @BotFather y obtener chat_id con @userinfobot
TELEGRAM_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=123456789

# --- RCLONE (Google Drive) ---
# Nombre del remote configurado en rclone + ruta de destino
# Nota: rclone debe estar configurado previamente (ver dotfiles)
RCLONE_REMOTE=gdrive:Vaultwarden

# --- RETENCIÓN DE BACKUPS ---
# Días a mantener los backups en la nube (por defecto 7)
BACKUP_RETENTION_DAYS=7
